apiVersion: v1
kind: Service
metadata:
  name: kubewatch-frontend
  namespace: kubewatch
  labels:
    app.kubernetes.io/name: kubewatch-frontend
    app.kubernetes.io/part-of: kubewatch
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kubewatch-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
