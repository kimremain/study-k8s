spec:
  template:
    spec:
      containers:
        - name: backend
          env:
            - name: KUBEWATCH_API_KEY
              valueFrom:
                secretKeyRef:
                  name: kubewatch-backend-secret
                  key: KUBEWATCH_API_KEY
